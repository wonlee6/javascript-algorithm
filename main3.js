// 1. 회문 문자열 찾기
// 회문문자열이란 ? 앞과 뒤가 같은 문자열
// ex) gooG -> 회문문자열
// yes or no 로 출력 단, 대소문자 구분하지 않음
function solution01(arr) {
  let answer;

  return answer;
}
let word = "gooG";
console.log(solution01(word)); // yes

// 2. 유효한 펠린드롭
// 앞에서 읽나 뒤에서 읽나 같은 문자열을 펠린드롭이라고 한다
// 입력 문자열이 펠린드롭이면 yes 아니면 no 출력
// 대소문자 구분하지 않음, 알파벳 이외의 문자 무시
function solution02(word) {
  let answer;

  return answer;
}
const word02 = "found7, time: study; Yduts; emit, 7Dnuof";
console.log(solution02(word02)); // yes

// 3. 숫자만 출력(자연수로)
//

function solution(arr) {
  let answer;
  return answer;
}

const word03 = "g0en2T0s8eSoft";
console.log("3. ", solution(word03)); // 208

// 4. 가장 짧은 문자거리
// e 로부터 떨어진 거리 구하기
function solution04(words, word) {
  let answer;

  return answer;
}

const word04 = "teachermode";
const wordd = "e";
console.log(solution04(word04, wordd)); // 1 0 1 2 1 0 1 2 2 1 0

// 5. 문자열 압축 (중복제거) 후 숫자표시
// 문자 하나 일 경우 생략
function solution05(word) {
  let answer;
  return answer;
}
const word05 = "KKHSSSSSSSE";
console.log(solution05(word05)); // K2HS7E
